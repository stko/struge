page: '<html>

  <head>

      <title>{{ item.title }}</title>

  </head>

  <body>

     {{ inner }}

  </body>

  </html>'


header: '<{{ item.size }}>{{ item.text }}</{{ item.size }}>'

list: '<ul>
    
    {% for li in item.list_items %}
    <li>{{ li }}
    
    {% endfor %}
    </ul>'

